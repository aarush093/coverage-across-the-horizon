DOC: 43_REVIEWER_2 | OWNER: Shared | CADENCE: as insight arrives
STATUS: active | LAST-UPDATED: 2026-08-29 | SUPERSEDES: 2026-07-17 seed

# 43 — REVIEWER #2 (anticipated attacks + banked responses)

| ID | Anticipated attack | Response (banked) | Source |
|----|--------------------|-------------------|--------|
| RV01 | "Why not just use DeRegiME? Same suite, PatchTST/DLinear encoders, handles regime shift." | DeRegiME is a TRAINED sparse-GP density model optimising NLPD/CRPS. Ours is distribution-free, post-hoc, model-agnostic, O(1), with coverage CONTROL and a decision-level eval. Different objective: density quality vs guaranteed coverage. | verified 2605.19231, 2026-07-17 |
| RV02 | "Your conditional-coverage guarantee is an overclaim (F5)." | Theory-Scope box claims ONLY ACI-family long-run adaptive coverage + approximate bucketed conditioning. Never finite-sample or exact conditional coverage. | Lock F5/R4 |
| RV03 | "Per-cell 90% ≠ 90% whole-path (F6)." | Both reported; joint coverage quantified with the width cost of each guarantee. | Lock F6 |
| RV04 | "MSCP is your contribution relabeled." | MSCP is an explicit BASELINE and empirically a *bad* one here — worst-cell 0.7251, below the do-nothing Global at 0.7667. The claim is the conditioning×adaptation interaction. | Lock F1 + results.json |
| RV05 | "Coverage numbers corrupted by drop_last / overlapping windows." | §10 hygiene: drop_last off, stride-24 calibration, sequential splits, published seed. | Lock F3/F7 |
| **RV06** | **"You claim model-agnosticism from a swap between DLinear and NLinear — two one-layer linear maps from the same paper. That is not a swap."** | **This attack lands. Do not bluff it.** Options: (a) reinstate PatchTST before S6 — the honest fix; (b) narrow C2 in writing to "backbone-independent given a frozen point forecaster, demonstrated across two linear backbones" and name the limitation before the reviewer does. Currently the evidence is Δ worst-cell 0.0013, which is consistent with model-agnosticism but tests almost nothing. | D007, 2026-08-29 |
| **RV07** | **"Your method has the WORST whole-path coverage of all seven: joint 0.0005 vs Gaussian's 0.0045."** | True, and the mechanism is honest: Proposed's intervals are the narrowest (1.9245 vs Global 2.0857), and joint whole-path coverage is dominated by width. This is exactly F6's point — per-cell and whole-path are different targets and a method optimised for one loses the other. Report it as a finding, not a footnote: *at H up to 720, whole-path coverage collapses toward zero for every method tested, so per-cell conditional coverage is the only guarantee worth claiming at these horizons.* | results.json, 2026-08-29 |
| **RV08** | **"On Electricity your minimum cell is 0.236 — worse than the Global baseline's 0.340. Your method fails harder."** | Also true, and the distribution tells the rest: within ±0.05 of target 274/300 vs 142/300, below 0.80 10 cells vs 40. Failures concentrate in the smallest log-spaced horizon bucket, which holds few steps and so few samples. Fix or disclose: either floor the bucket size or report `cell_p05` alongside the min. **[UNVERIFIED — these numbers are not currently reproducible; see FR01.]** | 2026-08-29 |
| **RV09** | **"Where is your related work? The 2026 conformal-forecasting literature moved while you ran experiments."** | No banked response. S0 was never exited (R8). 2604.13253 — bias-corrected ACI for multi-horizon forecasting, the closest thing to W2 — is unread. This is the single most exposed part of the project. | 12_PROGRESS_TRACKER |
| **RV10** | **"n_test = 117 windows. Worst-cell over 42 cells on 117 windows is noise."** | Partial response: stride-24 is a deliberate F3 hygiene choice to kill residual dependence, and the trade is real. Needs a sensitivity check — worst-cell as a function of stride is already in `stride_abl`; pull that forward into the paper rather than leaving it in the appendix. | Q07, 2026-08-29 |
