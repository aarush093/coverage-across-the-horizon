DOC: 43_REVIEWER_2 | OWNER: Shared | CADENCE: as insight arrives
STATUS: active | LAST-UPDATED: 2026-08-30 (rev5) (rev2) | SUPERSEDES: rev1 (2026-08-30)

# 43 — REVIEWER #2 (anticipated attacks + banked responses)

| ID | Anticipated attack | Response (banked) |
|----|--------------------|-------------------|
| RV01 | "Why not just use DeRegiME?" | Trained sparse-GP density model optimising NLPD/CRPS. Ours is distribution-free, post-hoc, model-agnostic, O(1), with coverage CONTROL and a decision-level eval. Different objective. |
| RV02 | "Your conditional guarantee is an overclaim (F5)." | Theory-Scope box claims ONLY ACI-family long-run adaptive coverage + approximate bucketed conditioning. Never finite-sample or exact conditional coverage. |
| RV03 | "Per-cell 90% ≠ 90% whole-path (F6)." | Both reported, with the width price quantified — see RV07. |
| RV04 | "MSCP is your contribution relabeled." | MSCP is a baseline and empirically a bad one: 0.7251 on ETT and 0.5382 on Electricity, both **below** the do-nothing Global. |
| RV05 | "drop_last / overlapping windows corrupt this." | §10 hygiene: drop_last off, strided calibration, sequential splits, published seed. |
| RV06 | **"DLinear→NLinear is not a backbone swap — same paper, both one-layer linear."** | **Lands. Do not bluff.** Either reinstate PatchTST (P03) or narrow C2 in writing. Current evidence: Δ worst-cell 0.0013 on ETT, 0.0058 on Electricity. Consistent with model-agnosticism; tests very little. **Strengthened 2026-08-30 by EXP_S4_008.** The two backbones are not interchangeable: DLinear carries persistent per-cell bias (r = +0.579 / +0.731, growing 0.076 -> 0.257 across H); NLinear carries essentially none (r = -0.044 / -0.595). That the calibration layer reaches 0.8664 and 0.8651 worst-cell across two backbones with *materially different* error processes is better evidence for C2 than two backbones that merely share a name. It does not replace PatchTST (P03), but it is a real defence rather than an assertion. |
| RV07 | "Your method has the WORST whole-path coverage." | True on ETT (0.0005 vs Gaussian 0.0045) and on Electricity every per-step method is 0.000. Reframe as the finding: **at H≥96 over 50 channels, whole-path coverage collapses for every per-step method, and buying it back costs ≈10× width** (MaxScore 0.944 at H=96). Per-cell conditional coverage is the only guarantee worth claiming at these horizons. |
| RV08 | "On Electricity your minimum cell is worse than baseline." | **Superseded by the reproducible run.** On the committed data Proposed's worst cell is 0.8306 vs Global 0.5757 — Proposed is *better*, not worse. The earlier contrary claim came from lost, unreproducible code (FR01) and is withdrawn. Cells below 0.80: Proposed 0.0017 vs Global 0.0488. |
| RV09 | **"Where is your related work?"** | **No banked response.** S0 never exited; 2604.13253 unread. Highest-priority gap. |
| RV10 | "n_test = 117 (ETT) / 190–216 (Electricity). Worst-cell on that is noise." | Partial: stride is a deliberate F3 choice. Mitigation now in place — report `cell_p05`, `frac_within_5pt` and `frac_below_80` alongside the raw minimum (D014), since a minimum over 300 cells and over 42 cells are not comparable quantities. Pull the `stride_abl` sensitivity forward from the appendix. **Amended 2026-08-30 — the same non-comparability bit us inside our own K ablation, not just across surfaces.** `run_experiments.py` scores each K on its OWN K-bucket grid, so K=1 is a minimum over 7 cells and K=10 over 70; the coarse grid wins almost by construction. Re-scored on a fixed K=6 grid the conclusion **reverses**: own-grid says best K=1 and K=6 beats K=1 in **0/32** configs, fixed-grid says K=6 and **31/32**. Both scorings agree exactly at K=6 where the grids coincide (0.865746 vs committed 0.8657). Fix is `scripts/run_horizon_ablation.py`, logged as **FR04**; `results.json`'s `kabl` block is **superseded, not deleted**. |
| RV11 | **"You optimise per-cell coverage and then report per-cell coverage. Circular."** | Not circular, and the data shows why. ACI adapts toward the same target unconditionally → 0.6177. Cond conditions on the same grid without adapting → 0.4621. Only the combination reaches 0.8306. If the metric were simply being optimised, either factor alone would approach it. Also, Proposed does **not** hit target: worst-cell 0.8306 against 0.90, with real failures concentrated in the smallest log-spaced bucket. |
| RV12 | **"Your decision advantage comes from having intervals, not from conditioning."** | Largely true, and stated as such. Point 0.362 → Global 0.175 is the big step; Global 0.175 → Proposed 0.167 is small. What survives: the direction is consistent in **8/8** configs, and the margin widens as the cost ratio rises (at 50:1, 0.076 → 0.062). **And the ordering reverses at 2:1, where the point forecast wins (0.463 vs 0.692).** The honest claim is conditional: interval-gating pays when missing a peak costs ≳5× a false alarm. |
| RV13 | **"On the worst meter your intervals are worse than doing nothing."** | True and reported. At ratio 10: Point 1.00, Global 2.04, Proposed 2.35. Recall rises 0.663 → 0.964 but the flag rate rises 14% → 35%, and on the worst meter that false-alarm load exceeds the benefit. This is the decision-side counterpart of the coverage-side result and it goes in the paper, not a footnote. |
| RV14 | **"Even your joint method fails at long horizon."** | True: MaxScore whole-path coverage decays 0.944 (H=96) → 0.842–0.895 (H=720) while still paying ≈10× width. Report it — it bounds what any post-hoc layer can promise at H=720 and is a genuine contribution to W3 rather than an embarrassment. |
| **RV15** | **"Your method is called horizon-AND-channel conditional but your own delta note says the channel axis is the one that breaks — why isn't this just channel-conditional adaptive conformal?"** | **Fair from DELTA_001 alone, and DELTA_001 was wrong. Answered directly, because the ablation that answers it is now run (EXP_S4_007 / EXP_S5_008).** (1) **The question is exactly testable.** `buckets(H,1)` collapses the horizon term, so "channel-conditional adaptive conformal" IS our method at K=1 — verified structurally, Cond@K=1 ≡ CondC exactly in **32/32** ETT and **8/8** Electricity configs. It is a K value, not a rival method. (2) **On ETT, dropping the horizon axis costs 0.0394 worst-cell** (0.8657 → 0.8263), losing in **31/32** configs. That is ~30% of the method's entire margin over the best static baseline (0.8657 − 0.7797). (3) **The reviewer's premise inverts under adaptation.** Adding the horizon axis *hurts* the static arm (−0.0209 ETT, −0.0853 Electricity) and *helps* the adaptive one (+0.0394 ETT) — a second interaction worth **+0.0603**, distinct from the conditioning×adaptation **+0.1291**. DELTA_001 read the static half correctly and generalised it to an adaptive case that had not been run. **Do not bluff the Electricity half.** There the adaptive gain is **−0.0034, i.e. absent on worst-cell (4/8)**, and it splits cleanly by horizon: K=1 wins at H=96/192, K=6 at H=336/720, same for both backbones. Every *distributional* metric still prefers K=6 (within ±5pt **0.625 → 0.977**, Winkler 1.4220 → **1.4083**, and slightly narrower). **Honest claim: the horizon axis earns its name on ETT at all horizons and on Electricity from H≥336 and on grid distribution throughout; on the wide surface at short horizon it is not carrying the worst-cell number.** (4) The name stays; the paper reports the reversal rather than averaging it away. See DELTA_002 §3 and Q18. |
| **RV16** | **"Your intervals are symmetric around a frozen point forecast. Under the seasonal shift you built into S5, a stale forecaster develops persistent bias, and BC-ACI (arXiv:2604.13253) shows symmetric intervals then pay a width overhead of roughly twice the bias. Did you check?"** | **Yes -- measured, and the answer splits by backbone, so do not quote the pooled number.** `scripts/check_bias.py`, EXP_S4_008, per-cell mean residual with a leakage-free re-centring estimated on the calibration block only. **DLinear: the premise holds.** Mean abs(b) is 16.5% of interval width on ETT and it persists -- calibration bias predicts test bias at r = **+0.579** (ETT) and **+0.731** (Electricity), growing with horizon (0.076 -> 0.257 from H=96 to H=720). Worst case DLinear/ETTh2/H=720: abs(b) = 37% of the interval, and re-centring returns **11.2%** of width. **NLinear: the premise fails.** abs(b) is 4.2% of width and does not persist (r = **-0.044** ETT, **-0.595** Electricity). Mechanism: NLinear subtracts the last observed value and re-anchors every window; DLinear decomposes trend and seasonality and drifts. **Why the attack still does not land:** re-centring buys width, not coverage. Even on the worst config it moves worst-cell the *wrong* way (0.8547 -> 0.8451); pooled it is -1.52% width for -0.0009 worst-cell. Our target is conditional coverage and a bias correction does not touch it. **State the limitation honestly:** on trend-decomposing backbones at long horizons our intervals are wider than they need to be by up to ~11%, and BC-ACI is the right composable fix -- it acts on the score, our conditioning acts on the quantile. |
| **RV17** | **"The nearest benchmark in the literature (arXiv:2601.18509) concludes MSCP is the best method. You rank it last. Why should we believe you?"** | Because we are not measuring the same thing, and the disagreement is the contribution. That benchmark uses AutoARIMA on monthly sales and scores marginal coverage, width and Winkler. On those metrics MSCP looks excellent on our surface too — it has the **highest marginal coverage of all seven methods (0.9123)**. It is only on conditional coverage over the horizon-bucket × channel grid that it collapses to **worst-cell 0.7251**, below the do-nothing Global baseline at 0.7667. Both results are correct. A benchmark that reports only marginal statistics cannot see the failure, which is the entire argument for W1. Cite 2601.18509 approvingly — it is careful work within its declared scope. |
| **RV18** | **"Your horizon-bucket conditioning isn't capturing scale structure -- it's soaking up your backbone's horizon-dependent bias. Any per-horizon offset would do the same."** | **Tested and rejected, using the two backbones as a natural experiment.** DLinear carries 3.5x NLinear's per-cell bias (mean abs(b) 0.1514 vs 0.0436, EXP_S4_008) and its bias persists while NLinear's does not (r = +0.579 vs -0.044). If the horizon effect were bias absorption it would be far larger on DLinear. It is not: **+0.0406 on DLinear against +0.0383 on NLinear**, a gap of 0.002. Per-config the association is slightly negative, r(abs(b), horizon effect) = **-0.317** over 32 configs. Independent corroboration: a direct bias correction returns up to 11.2% of width on the worst DLinear config and still moves worst-cell the wrong way. Bias and conditional coverage are decoupled here. The horizon axis captures **scale** heterogeneity, which is what DELTA_001's periodicity finding predicts: per-horizon width has lag-24 autocorrelation +0.9810 and is not monotone in h. |
| **RV19** | **"You call the method adaptive, but every result is a block average. An average cannot tell a method that holds target from one that over-covers then collapses. Show me adaptation happening."** | **MET02, EXP_S4_005 -- the clearest evidence in the project.** On Electricity, whose test block runs from late spring through winter, static global conformal spends **17.5%** of the period below 0.85 coverage with dips lasting **~3 weeks**; ACI and Proposed spend **0.0%** there, longest dip **zero**. The static failure is not diffuse: Global's worst 30-day window falls at test path **~163-164 in 7 of 8 configs**, the same point for both backbones -- a property of the data, not the model. It deepens with horizon to **0.7750** at H=720 while Proposed holds **0.8579** through the identical window on identical forecasts. **Stated against ourselves:** on ETT, whose split does not cross a season, the effect is much weaker (Global 16.0% below 0.85, Proposed 11.9%) and **ACI alone is worse than doing nothing at 17.1%**. Adaptation without conditioning is not a free win -- the interaction claim again, from a third direction. |
