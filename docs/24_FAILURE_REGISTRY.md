DOC: 24_FAILURE_REGISTRY | OWNER: Shared | CADENCE: per-failure
STATUS: active | LAST-UPDATED: 2026-08-30 | SUPERSEDES: 2026-08-29 version

# 24 — FAILURE REGISTRY (what broke & why)
| ID | Stage | What failed | Root cause | Fix / verdict | Date |
|----|-------|-------------|-----------|---------------|------|
| FR01 | S4/S5 | An entire session's code layer was lost: `coverage_horizon/decision.py`, `shift.py`, `data/electricity.py`, `data/bdg2.py`, `scripts/merge_results.py`, `scripts/run_casestudy.py`, sharding edits to `run_experiments.py`, and the new `cell_p05` / `frac_within_5pt` metrics. | Work was done inside an ephemeral sandbox and never committed or handed to the user. The session hit a tool limit before the push step, and the container was wiped. [FACT — repo HEAD verified as `42be4a5` on 2026-08-29, none of the files present.] | **D008 is now binding: hand over or commit code BEFORE running it, every time.** The S5 numbers produced in that session are downgraded to [UNVERIFIED] until regenerated from pushed code. | 2026-08-29 |
| FR02 | docs | `12_PROGRESS_TRACKER` read "S1–S6 NOT STARTED" for ~6 weeks while S1–S3 were completed and Review 1 was delivered. | Cadence is "weekly"; it was never run. Doc drift is silent by construction. | Tracker rebuilt 2026-08-29. Treat any doc whose LAST-UPDATED is >3 weeks old as untrusted until reconciled. | 2026-08-29 |
| FR03 | recall | A working summary carried "conditioning alone = 0.652". The committed `results.json` gives Cond = **0.7588**. | A number was carried in prose across sessions instead of being re-read from the artefact. | Any headline number must be recomputed from `results.json` before it enters a document. Correct 2×2: Global 0.7667 / Cond 0.7588 / ACI 0.7445 / Proposed 0.8657. | 2026-08-29 |
| **FR04** | S4 | The K ablation in `scripts/run_experiments.py` (`kabl` block) compared bucket counts on **worst-cell coverage scored on each K's own grid**. It reported K=4/6/8 tied at 0.8571 and K=10 lower at 0.8516, which reads as "K does not matter, and more buckets slightly hurt". Re-scored on a fixed grid the ordering **reverses**: K=6 beats K=1 in **31/32** configs, and **0/32** under own-grid scoring. | Worst-cell is a **minimum over cells**, and the cell count is K×C — 7 cells at K=1 against 70 at K=10. A minimum over fewer, larger cells is higher almost by construction, so the statistic rewards coarseness independently of the calibration layer. Two different estimators were being compared as if they were two different methods, and the bias runs in the direction that flatters small K. Own-grid mean is monotonically decreasing in K (0.8717 → 0.8616) while fixed-grid is not. **Same family as D009 and RV10 — a minimum is only comparable at fixed grid width — but it went undetected inside a single script's own ablation rather than across surfaces.** | **Fixed by `scripts/run_horizon_ablation.py`**, which scores every configuration twice: `worst_own` on its own K-grid and `worst_ref` on a fixed K_REF=6 grid. Only `worst_ref` is comparable across K; `worst_own` is retained solely to size the confound. The two agree exactly at K=6, where the grids coincide (0.865746 vs committed 0.8657), which is the consistency check that the new path did not move anything. `results.json` is **untouched** and its `kabl` block is reported as **superseded, not deleted** — it is single-config (DLinear/ETTh1/H=720), covers K ∈ {4,6,8,10} only, and is own-grid scored; not wrong for what it is, just not a basis for choosing K. **General rule: never compare a min/max order statistic across configurations that change the number of cells it is taken over.** | 2026-08-30 |

Watch-list from Lock F-log: drop_last truncation (F7) corrupts coverage silently; overlapping calibration windows (F3) bias optimistically; 720 independent quantiles (F4) explode variance.
